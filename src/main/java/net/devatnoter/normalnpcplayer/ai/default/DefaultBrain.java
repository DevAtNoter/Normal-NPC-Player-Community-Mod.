package net.devatnoter.normalnpcplayer.ai.defaultbrain;

import net.devatnoter.normalnpcplayer.entity.AdultPlayerMobEntity;

/** Empty brain reserved for future controllable Adult Player Mob behavior. */
public final class DefaultBrain {
    private DefaultBrain() {}

    public static void registerGoals(AdultPlayerMobEntity entity) {
        // Intentionally empty.
    }

    public static void tick(AdultPlayerMobEntity entity) {
        // Intentionally empty.
    }
}
